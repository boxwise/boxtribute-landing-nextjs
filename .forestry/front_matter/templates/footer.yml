---
label: footer
hide_body: true
fields:
- name: articles_of_association
  type: file
  config:
    maxSize: 64
  label: Articles of Association
- name: policy_plan
  type: file
  config:
    maxSize: 64
  label: Policy Plan
- name: remuneration_policy
  type: file
  config:
    maxSize: 64
  label: Remuneration Policy
- name: annual_reports
  type: field_group_list
  fields:
  - name: year
    type: text
    config:
      required: true
    label: Year
  - name: report
    type: file
    config:
      maxSize: 64
    label: Annual Report
    description: Please be aware that the name of the pdf will be the same for the
      user.
  config:
    min: 1
    max: 
    labelField: 
  label: Annual reports
- name: financial_reports
  type: field_group_list
  fields:
  - name: year
    type: text
    config:
      required: true
    label: Year
  - name: report
    type: file
    config:
      maxSize: 64
    description: Please be aware that the name of the pdf will be the same for the
      user.
    label: Financial Report
  config:
    min: 1
    max: 
    labelField: 
  label: Financial Reports
pages:
- data/footer/footer.md
