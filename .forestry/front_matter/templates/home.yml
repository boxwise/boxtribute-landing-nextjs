---
label: home
hide_body: true
fields:
- name: main_heading
  type: text
  config:
    required: false
  label: main heading
- name: hero_image_description
  type: text
  config:
    required: false
  label: hero image description
- name: subtitles_hero
  type: field_group_list
  fields:
  - name: text_for_typing
    type: text
    config:
      required: false
    label: text for typing
  config:
    min: 
    max: 
    labelField: 
  label: subtitles hero
- name: hero_image_mobile
  type: file
  config:
    maxSize: 64
  description: The image needs to be quadratic and have a width and height of 800px
  label: hero_image Mobile
- name: hero_image_desktop
  type: file
  config:
    maxSize: 64
  label: hero_image Desktop
  description: 'The image needs to have a width of 1200px and a height of 675px '
- name: cta_button
  type: text
  config:
    required: false
  label: CTA button
- name: cta_link
  type: text
  config:
    required: false
  label: CTA link
- name: short_description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: short description
- name: facts_in_numbers
  type: field_group_list
  fields:
  - name: icon
    type: file
    config:
      maxSize: 64
    label: icon
    description: Please only upload either a quadratic image of 96px or a quadratic
      svg!
  - name: description
    type: text
    config:
      required: false
    label: description
  - name: number
    type: number
    label: number
    description: ''
    default: 0
    required: true
    config:
      min: 
      max: 
      step: 
  - name: image_description
    type: text
    config:
      required: false
    label: image_description
  config:
    min: 
    max: 
    labelField: 
  label: facts in numbers
pages:
- data/home/home_data.md
