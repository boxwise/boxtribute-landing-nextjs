---
label: report
hide_body: true
fields:
- name: year
  type: text
  config:
    required: false
  label: Year
- name: report
  type: file
  config:
    maxSize: 64
  label: Report PDF
  description: Please be aware that the title of the pdf will be the same when a user
    downloads the pdf.
